# DENY ALL — this is a public profile repo.
# Only explicitly allowed files can be tracked.
# To add a new file: add a !filename line below, then git add.
*

# Allowed files
!README.md
!SECURITY.md
!.gitignore
!commitlint.config.js

# Allowed directories (must un-ignore dir then contents)
!.github/
!.github/**
